# Project Status

## Repository
- **Directory**: `/Users/hariprasathc/Projects/AppleCareAI`
- **Git**: Not initialized. No `.git` repository present.
- **Project Structure**: Only a `datasets` directory exists. No frontend, backend, or ML code exists yet.

## Environment
- **OS/Runtime**: macOS, Python 3.14.7 (Miniconda)
- **Packages**: Minimal standard Anaconda base packages. Missing ML/Data dependencies (`pandas`, `scikit-learn`, `sentence-transformers`, `flask`, `pytest`).

## Dataset
- **Location**: `datasets/twcs.csv` (~516MB) and `datasets/sample.csv` (17KB).
- **Status**: The full dataset is present locally. No download from Kaggle is required. The columns match the assignment specification (`tweet_id`, `author_id`, `inbound`, `created_at`, `text`, `response_tweet_id`, `in_response_to_tweet_id`).
